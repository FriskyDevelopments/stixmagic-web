{
  "name": "telegram-videochat-companion-bot",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "bot",
    "core",
    "web"
  ],
  "scripts": {
    "dev": "bash -lc 'npm run dev:bot & npm run dev:web & wait'",
    "dev:bot": "npm run dev -w bot",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w core && npm run build -w bot && npm run build -w web",
    "start": "npm run start -w bot",
    "typecheck": "npm run typecheck -w core && npm run typecheck -w bot && npm run typecheck -w web"
  }
}
